<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.aitenry.iims.archive.mapper.ArchiveHashMapper">

    <select id="getMapperById">
        SELECT * FROM iims_archive_mapper WHERE organization_final_id = #{id}
    </select>

</mapper>